# Codex Instructions

Follow `agents/AGENTS.md`. Use `openapi.yaml` as the schema contract and prefer `@odds-api/client` for TypeScript data access. Do not copy repo examples by default; use them only as optional references for API shape, mock mode, and safety handling.
